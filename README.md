# The website at kinkybangkok.com

## Development

To run locally, make sure you have pnpm installed.

```
pnpm i
npm start
```

## Production

This runs on Netlify. CI is already setup. For it to update, just commit to `main`.
